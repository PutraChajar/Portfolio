import Routes from './Routes';
import store from './Redux/store';

const CONFIG = {
    BASE_URL: 'https://putrachajar.com/',
    BASE_URL_API: 'http://localhost:4000/',
    VERSION_API: 'v1',
};

export {Routes, store, CONFIG};